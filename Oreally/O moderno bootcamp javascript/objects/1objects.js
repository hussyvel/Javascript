let myBooks = {
    title:'1984',
    author:'George Orwell',
    pageCount: 326
}

console.log(`${myBooks.title}`)

myBooks.title='Animal Farm'

console.log(`${myBooks.author}`)


let lives = {
    nome:'Hussyvel',
    age:33,
    location:'São Luís'
}


console.log(`${lives.nome} is ${lives.age} lives ${lives.location}`)

lives.age = lives.age + 2
console.log(`${lives.nome} is ${lives.age} lives ${lives.location}`)