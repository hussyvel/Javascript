// 1. Você não pode definir uma variável mais que uma vez
let petName = 'Hus'
petName = 'Spot'

console.log(petName)

// Estas regras são relativas para nomes de variáves
let test = 2
let result = 3 + 4

// Nomes de variáveis não podem usar palavras reservadas
//let let = 12
